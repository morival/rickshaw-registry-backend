const express = require('express');
const ObjectID = require('mongodb').ObjectId;
const bcrypt = require('bcrypt')

const createAdminRouter = function(collection) {

    const router = express.Router();

    


    return router;
}

module.exports = createAdminRouter;